public without sharing class PartnerProgram_TierUpgrade {

    //
    // This is used to tell other triggers that we just approved this tier change, so don't mess with it!
    //

    public static Boolean isTierUpgradeApproved = false;

}